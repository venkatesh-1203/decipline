{
  "name": "digitalproduct-client",
  "version": "1.0.0",
  "private": true,
  "description": "Static frontend build tooling for the digital product site",
  "scripts": {
    "build:css": "tailwindcss -i ./client/input.css -o ./client/styles.css --minify",
    "watch:css": "tailwindcss -i ./client/input.css -o ./client/styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
